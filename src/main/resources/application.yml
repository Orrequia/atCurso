logging:
  level.org.springframework: INFO
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/atCurso?useSSL=false
    username: root
    password: 
---
spring:
  profiles: local
  datasource:
    url: jdbc:mysql://localhost:3306/atCurso?useSSL=false
    username: root
    password: 
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: create-drop
      dialect: org.hibernate.dialect.MySQL5Dialect